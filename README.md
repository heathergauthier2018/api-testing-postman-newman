# 🍸 API Testing Suite – TheCocktailDB (Postman + Newman)

This project is an automated API testing suite for [TheCocktailDB](https://www.thecocktaildb.com/), built with **Postman** and executed via **Newman**.

It is designed both as a **professional portfolio piece** and as a **practical regression suite** for any app that consumes this API (including my Cocktail Finder UI project).

---

## ✨ What this project demonstrates

- Designing a **structured Postman collection** for a real public API
- Writing **automated Postman tests**, including:
  - Functional / happy-path scenarios  
  - Negative & edge-case tests  
  - Basic performance checks  
- Using **collection variables** for maintainable URLs  
- Executing tests via **Newman** (CI-friendly)  
- Generating a visual **HTML report** with `newman-reporter-htmlextra`  
- Building a full API regression suite ready for CI/CD  

---

## 🧱 Tech Stack

- **Postman** – request definitions & JavaScript test scripts  
- **Newman** – command-line Postman runner  
- **newman-reporter-htmlextra** – HTML reporting dashboard  
- **Node.js + npm** – dependency & script management  
- **JavaScript / ChaiJS** – assertion layer inside Postman  

---

## 📂 Project Structure

```text
api-testing-postman-newman/
├─ postman/
│  ├─ collections/
│  │  └─ cocktail-api-testing-collection.json   # Postman collection with all tests
│  └─ environments/                             # reserved for future use
├─ reports/
│  └─ cocktail-api-report.html                  # generated by Newman
├─ node_modules/
├─ package.json
├─ package-lock.json
└─ README.md


```

---

## 🌐 API Under Test

All requests target:

```
https://www.thecocktaildb.com/api/json/v1/1
```

Stored as the Postman variable:

```
baseUrl = https://www.thecocktaildb.com/api/json/v1/1
```

---

## 🧪 Endpoints & Scenarios Covered

Below is the complete test coverage included in this suite.

### **1. Search Cocktails by Name**
```
GET /search.php?s=margarita
```
Tests:
- Status 200  
- Response time < 800ms  
- JSON response  
- `drinks` exists and is non-empty  

---

### **2. Get Cocktail by ID**
```
GET /lookup.php?i=11007
```
Tests:
- Status 200  
- JSON response  
- `drinks` array not empty  
- First drink has `idDrink = "11007"`  

---

### **3. Invalid Cocktail Name (Negative Case)**
```
GET /search.php?s=sdjasdjhasdjk
```
Tests:
- Status 200  
- `drinks` is null or empty  

---

### **4. Filter Cocktails by Category**
```
GET /filter.php?c=Cocktail
```
Tests:
- Status 200  
- Response time < 800ms  
- JSON response  
- Drinks array not empty  
- Each drink has: `idDrink`, `strDrink`, `strDrinkThumb`  

---

### **5. Filter Cocktails by Ingredient (Vodka)**
```
GET /filter.php?i=Vodka
```
Tests:
- Status 200  
- Response time < 800ms  
- JSON response  
- Drinks array not empty  
- Required fields exist  

---

### **6. Get Random Cocktail**
```
GET /random.php
```
Tests:
- Status 200  
- Response time < 800ms  
- JSON response  
- Drinks array exists  
- Required drink fields present  

---

### **7. Get Cocktail by Invalid ID (Negative Case)**
```
GET /lookup.php?i=9999999
```
Tests:
- Status 200  
- `drinks` null or empty  

---

### **8. Search with Empty Name (Edge Case)**
```
GET /search.php?s=
```
Tests:
- Status 200  
- `drinks` equals `"no data found"`  

---

### **9. Search with Uppercase Name (Case Insensitive)**
```
GET /search.php?s=MARGARITA
```
Tests:
- Status 200  
- Drinks array not empty  
- At least one name contains "margarita" (case-insensitive)  

---

### **10. Search with Space in Name (URL Encoding)**
```
GET /search.php?s=Bloody%20Mary
```
Tests:
- Status 200  
- Drinks array not empty  
- At least one `"Bloody Mary"` match  

---

### 📊 **Summary**
- **10 endpoints tested**  
- **34 total assertions**  
- **0 failures under Newman** 🎉  

---

## ▶️ Running the Suite with Newman

### **1. Install dependencies**
```
npm install
```

### **2. Run all tests**
```
npm run test:api
```

Equivalent command:

```
newman run postman/collections/cocktail-api-testing-collection.json \
  -r cli,htmlextra \
  --reporter-htmlextra-export reports/cocktail-api-report.html
```

---

## 📊 HTML Report

Newman generates a report at:

```
reports/cocktail-api-report.html
```

Open this file in any browser to view:

- Test run summary  
- Per-request details  
- Response times  
- Each assertion result  
- A clean visual dashboard  

---

## 🧩 Future Enhancements

- Add Postman environments (local, staging, mock server)  
- Integrate Newman tests with **GitHub Actions CI pipeline**  
- Add JSON schema validation  
- Introduce data-driven testing  
- Expand coverage to more TheCocktailDB endpoints  

---

## 🔗 Related Project

**Cocktail Finder UI** – A front-end app that consumes this API.  
(Will link once repo is public.)

---

## 👩‍💻 Author

**Heather Gauthier**  
API Testing • QA Automation • Software Engineering Student  
GitHub: @heathergauthier2018

